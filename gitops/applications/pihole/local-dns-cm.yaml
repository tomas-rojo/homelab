apiVersion: v1
kind: ConfigMap
metadata:
  name: pihole-local-dns
  namespace: pihole
data:
  # Format: "domain.com IP" (space-separated, one per line)
  # These map to /etc/pihole/custom.list inside the pod
  custom.list: |
    192.168.1.234  argocd.local
    192.168.1.234  longhorn.local
    192.168.1.234  pihole.local