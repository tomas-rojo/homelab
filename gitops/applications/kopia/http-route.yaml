apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kopia-route
  namespace: immich
spec:
  parentRefs:
  - name: gateway-api
    namespace: envoy-gateway-system
    sectionName: https
  hostnames:
  - kopia.home
  rules:
  - backendRefs:
    - name: kopia
      port: 51515
